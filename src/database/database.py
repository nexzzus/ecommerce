from src.database.config import engine, SessionLocal, Base, get_db

# This module re-exports the database objects from `src.database.config`
# to maintain backward-compatible imports used across the codebase.

